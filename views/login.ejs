<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: #f5f5f5;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 400px;
            padding: 40px;
        }
        h1 { font-size: 28px; margin-bottom: 8px; color: #333; }
        p { color: #666; font-size: 14px; margin-bottom: 30px; }
        label { display: block; margin-bottom: 6px; color: #333; font-size: 14px; font-weight: 500; }
        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            margin-bottom: 20px;
            transition: border-color 0.3s;
        }
        input:focus { outline: none; border-color: #333; }
        .forgot { text-align: right; margin: -10px 0 20px; }
        .forgot a { color: #333; text-decoration: none; font-size: 13px; }
        .forgot a:hover { text-decoration: underline; }
        .btn {
            width: 100%;
            padding: 12px;
            background: #333;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 15px;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.3s;
        }
        .btn:hover { background: #000; }
        .divider {
            text-align: center;
            margin: 25px 0;
            color: #999;
            font-size: 13px;
            position: relative;
        }
        .divider::before, .divider::after {
            content: '';
            position: absolute;
            top: 50%;
            width: 45%;
            height: 1px;
            background: #ddd;
        }
        .divider::before { left: 0; }
        .divider::after { right: 0; }
        .social { display: flex; gap: 10px; margin-bottom: 25px; }
        .social button {
            flex: 1;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background: white;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.3s;
        }
        .social button:hover { border-color: #333; background: #f9f9f9; }
        .link { text-align: center; color: #666; font-size: 14px; }
        .link a { color: #333; text-decoration: none; font-weight: 600; }
        .link a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome Back</h1>
        <p>Login to continue to your account</p>
        
        <label for="email">Email</label>
        <input type="email" id="email" placeholder="Enter your email">
        
        <label for="password">Password</label>
        <input type="password" id="password" placeholder="Enter your password">
        
        <div class="forgot"><a href="#">Forgot Password?</a></div>
        
        <button class="btn" onclick="login()">Login</button>
        
        <div class="divider">OR</div>
        
        <div class="link">Don't have an account? <a href="register.html">Sign Up</a></div>
    </div>

    <script>
        function login() {
            const e = document.getElementById('email').value;
            const p = document.getElementById('password').value;
            e && p ? alert('Login successful!') : alert('Please fill all fields');
        }
        document.getElementById('password').onkeypress = (e) => e.key === 'Enter' && login();
    </script>
</body>
</html>